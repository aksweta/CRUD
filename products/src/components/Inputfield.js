import React from "react";

const Inputfield = ({onchange , type, value, name, label}) => {
    <div>
         {label && <label htmlFor="input-feild">{label}</label>}
    
    <input type={} onchange={} value={} name={}/>
    </div>

};

export default Inputfield;